import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';

import Guest from '../components/Guest/Guest';

describe('Guest', () => {
  it('renders Guest elements', () => {
  });

});